const initialState = {
    tracks: null,
    fetchError: null,
    fetchLoading: null,
};

const tracksReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default tracksReducer;