const initialState = {
    albums: null,
    fetchError: null,
    fetchLoading: null,
};

const albumsReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default albumsReducer;